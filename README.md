# Tolksyn
Tolksyn is a mobile-first app for capturing or importing label images, extracting structured fields, confirming them, and sending JSON to an ingest endpoint. The primary loop is Capture -> Extract -> Confirm -> Send.
